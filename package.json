{
  "name": "medsi-timeweb-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Medsi web interface with a same-origin Timeweb gateway for chat traffic.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
