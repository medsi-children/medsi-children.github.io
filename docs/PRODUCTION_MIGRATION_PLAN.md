# Медси Бот — финальный план миграции в production

Дата фиксации: 6 сентября 2026

## Целевая архитектура

- Google Sheets + Apps Script — источник истины по детям, родителям, доступам и отчётам.
- Timeweb Node.js — production frontend/backend gateway и слой сессий.
- Cloudflare D1 — сообщения, реакции, прочитано/непрочитано, закрепления.
- Timeweb S3 — фото и видео чата.
- Cloudflare Push Worker — push-уведомления.
- GitHub — исходный код и autodeploy Timeweb.

## Правило миграции

До переключения домена ничего старого не удаляем. Старый Apps Script production остаётся рабочим аварийным вариантом до завершения smoke-test и нескольких дней стабильной работы новой схемы.

## Этап 0 — точка восстановления

- [x] Создать snapshot-ветку текущего рабочего Timeweb-кода: `pre-migration-snapshot-2026-09-06`.
- [ ] Не менять production `main` до финального cutover.

## Этап 1 — инвентаризация данных

- [ ] Снять актуальное состояние сообщений в текущем Apps Script / Google Sheets.
- [ ] Снять актуальное состояние Cloudflare D1.
- [ ] Посчитать сообщения и вложения по каждому телефону и определить расхождения.
- [ ] Зафиксировать текущие источники старых вложений: Google Drive / Cloudflare KV.

## Этап 2 — одноразовая миграция сообщений

- [ ] Перенести недостающие актуальные сообщения из текущего Apps Script / Sheets в Cloudflare D1.
- [ ] Не создавать дубликаты уже существующих сообщений.
- [ ] Сохранить автора, время, текст, reply, реакции, read/unread и остальные доступные метаданные.
- [ ] После миграции сверить количество и несколько реальных чатов вручную.

## Этап 3 — миграция вложений

- [x] Найти все вложения старой истории, которые ещё находятся в Google Drive / Cloudflare KV.
- [x] Скопировать их в Timeweb S3.
- [x] Обновить `fileId` / ссылки соответствующих сообщений в D1 на `s3:`.
- [ ] Проверить фото и видео из старой истории на родительской и воспитательской панели.
- [ ] Старые Drive/KV-копии пока НЕ удалять.

Проверено 6 сентября 2026: `CHAT_MESSAGES` и D1 содержат по 159
сообщений. В D1 — 16 изображений; все 16 ссылаются на `s3:`, ссылок
`kv:` и Drive больше нет. Каждый файл был прочитан обратно из Timeweb S3 и
сверен по SHA-256 до изменения записи D1. Оригиналы в Drive и 22 legacy
объекта в Cloudflare KV сохранены и не удалялись.

## Этап 4 — Google Sheets становится постоянным master-источником

После того как D1 и S3 приведены к актуальному состоянию:

- [ ] Добавить installable `onEdit` для листа `reports`.
- [ ] Добавить installable `onChange` для добавления/удаления строк и структурных изменений.
- [ ] Изменение телефона должно мигрировать D1-чат со старого номера на новый, включая ссылки на S3-вложения.
- [ ] Удаление ребёнка должно удалить его чат из D1 и вложения из S3.
- [ ] Изменение имени/прочих метаданных должно синхронизироваться без изменения истории сообщений.
- [ ] Ошибки синхронизации должны логироваться и не ломать редактирование таблицы.
- [ ] Эти триггеры не должны участвовать в пользовательских запросах бота и не должны влиять на скорость родительской/воспитательской панели.
- [ ] Добавить ручную функцию полного reconcile `reports -> D1/S3` на случай пропущенного события триггера.

## Этап 5 — уборка репозитория перед релизом

- [ ] Оставить в production-корне только актуальные Timeweb/parent/tutor/psychology/chat/server файлы.
- [ ] Неактуальные экспериментальные UI, diagnostics, старые варианты чата и миграционные утилиты перенести в `backups/`.
- [ ] Сохранить аварийную Apps Script-версию отдельно и явно пометить как rollback.
- [ ] PostgreSQL-хвосты убрать из активного runtime, поскольку production PostgreSQL не используется.
- [ ] Удалить или закрыть публичные диагностические endpoint'ы, которые не нужны после релиза.

## Этап 6 — подготовка домена

Production URLs:

- `https://медси-бот.рф/`
- `https://медси-бот.рф/tutors`
- `https://медси-бот.рф/psychology`

- [ ] Подготовить DNS/Timeweb без удаления старой конфигурации заранее.
- [ ] Убедиться, что SSL и все три URL готовы.
- [ ] Проверить сохранение/миграцию существующих сессий настолько, насколько это возможно на том же origin.

## Этап 7 — cutover

Выполнять только после явного подтверждения Дениса.

- [ ] Переключить `медси-бот.рф` на Timeweb.
- [ ] Проверить `/`, `/tutors`, `/psychology`.
- [ ] Production `main` и старый Apps Script не уничтожать.

## Этап 8 — smoke-test после переключения

Родитель:
- [ ] вход;
- [ ] отчёты;
- [ ] открытие чата;
- [ ] старая история;
- [ ] отправка текста;
- [ ] отправка фото;
- [ ] получение сообщения / push.

Воспитатель:
- [ ] вход/восстановление сессии;
- [ ] список чатов;
- [ ] открытие нескольких чатов;
- [ ] ответ;
- [ ] фото/видео;
- [ ] read/unread;
- [ ] закрепление;
- [ ] удаление ребёнка с очисткой D1 + S3.

Психолог:
- [ ] `/psychology` открывается;
- [ ] отчёт групповой психотерапии отправляется и форматируется корректно.

Отдельно:
- [ ] push-уведомления работают через Cloudflare Push Worker;
- [ ] рабочий Wi-Fi и мобильная сеть не дают зависаний.

## Этап 9 — период стабилизации

- [ ] Несколько дней не удалять legacy-инфраструктуру.
- [ ] Следить за ошибками Timeweb, D1, S3 и Apps Script sync-триггеров.
- [ ] При критической проблеме использовать snapshot/старую Apps Script production-схему.

## Этап 10 — последующая уборка

Только после стабильной работы:

- [ ] убрать старые UI Workers и тестовые upload Workers, если они больше не используются;
- [ ] убрать старые chat subdomains, если они больше не нужны;
- [ ] удалить старые KV/Drive-копии вложений только после полной проверки S3;
- [ ] оставить Cloudflare D1 и Push Worker;
- [ ] привести GitHub `main` к чистой production-структуре.
